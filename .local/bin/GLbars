#!/bin/sh

# Terminate
killall -q glava

# Wait until the processes have been shut down
while pgrep -u $UID -x glava >/dev/null; do sleep 1; done

# Launch
#while true; do echo fg = \#`mpc | head -n 1 | md5sum | cut -c 1-6`; sleep 0.35; done | glava --pipe=fg -m bars --desktop
glava --desktop &
