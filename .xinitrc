#!/bin/sh

if [ -d /etc/X11/xinit/xinitrc.d ]; then
  for f in /etc/X11/xinit/xinitrc.d/*; do
    [ -x "$f" ] && . "$f"
  done
  unset f
fi

xset +fp /usr/local/share/fonts/misc &
xrdb ~/.Xresources

exec berry 2> /tmp/xinitrc.log
